<UserControl x:Class="Dali.UI.DeviceManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:ui="clr-namespace:Dali.UI"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="600"
             Background="{DynamicResource PrimaryBackgroundBrush}">

    <UserControl.Resources>
        <ui:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <ui:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <ui:NullToCollapsedConverter x:Key="NullToCollapsedConverter"/>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Main Content Split -->
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Device Equipment Database" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
            <Button Grid.Column="1" Command="{Binding AddNewDeviceCommand}" Background="Transparent" BorderBrush="Transparent" Foreground="LightGreen" ToolTip="Add New Custom Device">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="PlusCircleOutline" Width="18" Height="18" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Add Device" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Main Split: Grid Left, Edit Right -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"/> <!-- Device List -->
                <ColumnDefinition Width="10"/> <!-- Splitter -->
                <ColumnDefinition Width="5*"/> <!-- Edit Form -->
            </Grid.ColumnDefinitions>

            <!-- LEFT: Device DataGrid -->
            <Border Grid.Column="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                <DataGrid ItemsSource="{Binding Devices}"
                          SelectedItem="{Binding SelectedDevice}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#333333"
                          RowBackground="Transparent"
                          Background="Transparent"
                          BorderThickness="0"
                          Foreground="{DynamicResource ForegroundBrush}"
                          ColumnHeaderStyle="{DynamicResource CustomDataGridColumnHeaderStyle}"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="Auto" CanUserSort="True"/>
                        <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="Auto" CanUserSort="True"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" CanUserSort="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="Transparent" />

            <!-- RIGHT: Edit Form -->
            <Border Grid.Column="2" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="15">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Edit Device Parameters" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,15"/>

                        <StackPanel Visibility="{Binding ActiveEditDevice, Converter={StaticResource NullToCollapsedConverter}}">
                            
                            <!-- ID (Read-only reference) -->
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="130"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Database ID" Foreground="Gray" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Text="{Binding ActiveEditDevice.Id}" IsReadOnly="True" Style="{DynamicResource CustomTextBoxStyle}" Foreground="DarkGray" Background="#111"/>
                            </Grid>

                            <!-- General Info -->
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="130"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Equipment Type" Foreground="Gray" VerticalAlignment="Center" ToolTip="e.g., controller, power_supply"/>
                                <TextBox Grid.Column="1" Text="{Binding ActiveEditDevice.Type, UpdateSourceTrigger=PropertyChanged}" TextChanged="EditBox_TextChanged" Style="{DynamicResource CustomTextBoxStyle}"/>
                            </Grid>

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="130"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Model Designation" Foreground="Gray" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Text="{Binding ActiveEditDevice.Model, UpdateSourceTrigger=PropertyChanged}" TextChanged="EditBox_TextChanged" Style="{DynamicResource CustomTextBoxStyle}"/>
                            </Grid>

                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="130"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Display Name" Foreground="Gray" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Text="{Binding ActiveEditDevice.Name, UpdateSourceTrigger=PropertyChanged}" TextChanged="EditBox_TextChanged" Style="{DynamicResource CustomTextBoxStyle}"/>
                            </Grid>

                            <!-- Divider -->
                            <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="0,5,0,15"/>

                            <!-- Controller specific capacities -->
                            <StackPanel Visibility="{Binding IsController, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="Controller Port Capacities" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,10"/>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="20"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid Grid.Column="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Lines Supported" Foreground="Gray" VerticalAlignment="Center" TextWrapping="Wrap"/>
                                        <TextBox Grid.Column="1" Text="{Binding ActiveEditDevice.DaliLines, UpdateSourceTrigger=PropertyChanged}" TextChanged="EditBox_TextChanged" Style="{DynamicResource CustomTextBoxStyle}"/>
                                    </Grid>
                                    <Grid Grid.Column="2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Max Addresses" Foreground="Gray" VerticalAlignment="Center" TextWrapping="Wrap"/>
                                        <TextBox Grid.Column="1" Text="{Binding ActiveEditDevice.MaxAddressesPerLine, UpdateSourceTrigger=PropertyChanged}" TextChanged="EditBox_TextChanged" Style="{DynamicResource CustomTextBoxStyle}"/>
                                    </Grid>
                                </Grid>

                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="20"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid Grid.Column="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Rated Output (mA)" Foreground="Gray" VerticalAlignment="Center" TextWrapping="Wrap"/>
                                        <TextBox Grid.Column="1" Text="{Binding ActiveEditDevice.RatedCurrentmAPerLine, UpdateSourceTrigger=PropertyChanged}" TextChanged="EditBox_TextChanged" Style="{DynamicResource CustomTextBoxStyle}"/>
                                    </Grid>
                                    <Grid Grid.Column="2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Guaranteed Output (mA)" Foreground="Gray" VerticalAlignment="Center" TextWrapping="Wrap"/>
                                        <TextBox Grid.Column="1" Text="{Binding ActiveEditDevice.GuaranteedCurrentmAPerLine, UpdateSourceTrigger=PropertyChanged}" TextChanged="EditBox_TextChanged" Style="{DynamicResource CustomTextBoxStyle}"/>
                                    </Grid>
                                </Grid>
                            </StackPanel>

                            <!-- Extender specific capacities (Future proofing) -->
                            <StackPanel Visibility="{Binding IsController, Converter={StaticResource InverseBooleanConverter}}">
                                <TextBlock Text="Power / Extender Additions" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,10"/>
                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="20"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid Grid.Column="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Adds Current (mA)" Foreground="Gray" VerticalAlignment="Center" TextWrapping="Wrap"/>
                                        <TextBox Grid.Column="1" Text="{Binding ActiveEditDevice.AddsCurrentmA, UpdateSourceTrigger=PropertyChanged}" TextChanged="EditBox_TextChanged" Style="{DynamicResource CustomTextBoxStyle}"/>
                                    </Grid>
                                    <Grid Grid.Column="2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Extends Length (M)" Foreground="Gray" VerticalAlignment="Center" TextWrapping="Wrap"/>
                                        <TextBox Grid.Column="1" Text="{Binding ActiveEditDevice.ExtendsLineLengthMetersTo, UpdateSourceTrigger=PropertyChanged}" TextChanged="EditBox_TextChanged" Style="{DynamicResource CustomTextBoxStyle}"/>
                                    </Grid>
                                </Grid>
                            </StackPanel>

                            <!-- Actions -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                                <Button Content="Delete Device" Command="{Binding DeleteDeviceCommand}" CommandParameter="{Binding ActiveEditDevice}" Style="{DynamicResource CustomCancelButtonStyle}" Height="35" Width="120" FontSize="13" Margin="0,0,10,0">
                                </Button>
                                
                                <TextBlock Text="* Unsaved Changes" Foreground="Gold" FontStyle="Italic" VerticalAlignment="Center" Margin="0,0,10,0"
                                           Visibility="{Binding HasChanges, Converter={StaticResource BoolToVisibilityConverter}}" />
                                <Button Content="Save Changes" Command="{Binding SaveDeviceCommand}" Style="{DynamicResource CustomActionButtonStyle}" Height="35" Width="120" FontSize="13" />
                            </StackPanel>
                        </StackPanel>

                        <!-- Fallback empty state -->
                        <TextBlock Text="Select a device from the list to view or edit its parameters." 
                                   Foreground="Gray" FontStyle="Italic" TextWrapping="Wrap"
                                   Visibility="{Binding ActiveEditDevice, Converter={StaticResource InverseBooleanConverter}, FallbackValue=Visible}"/>

                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
